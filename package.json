{
  "name": "@rugo-vn/vue",
  "version": "1.0.5",
  "type": "module",
  "main": "dist/library.js",
  "module": "dist/library.mjs",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build:library": "rollup -c",
    "build:icon": "node src/generate.js",
    "build": "npm run build:library && npm run build:icon",
    "clean": "rm -rf build && rm -rf dist && npm run example:clean",
    "doc:clean": "rm -rf docs/* && rm -rf example/src/components/docs/*",
    "doc:generate": "npm run build && node src/doc.js",
    "doc": "npm run doc:clean && npm run doc:generate",
    "example:install": "npm i && npm run build && npm run make-pack && cd example && npm i",
    "example:clean": "cd example && rm -rf node_modules && rm -rf package-lock.json",
    "example:build": "npm run example:clean && npm run example:install && cd example && npm run build",
    "example": "npm run example:clean && npm run example:install && cd example && npm run dev",
    "make-pack": "mkdir -p build && npm pack && mv rugo-vn-vue-* build/rugo-vn-vue.tgz",
    "publish:beta": "npm publish --access public --tag beta",
    "publish:release": "npm publish --access public",
    "deploy": "npm run example:build && gh-pages -d example/dist"
  },
  "peerDependencies": {
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "decamelize": "^6.0.0",
    "ejs": "^3.1.8",
    "gh-pages": "^5.0.0",
    "lorem-ipsum": "^2.0.8",
    "marked": "^4.2.12",
    "pascalcase": "^2.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.78.0",
    "rollup-plugin-copy": "^3.4.0",
    "rollup-plugin-peer-deps-external": "^2.2.4",
    "rollup-plugin-vue": "^6.0.0",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "uniqid": "^5.4.0"
  }
}
